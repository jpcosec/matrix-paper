# Raw Specs Archive

This directory preserves the source material used during the migration.

Current strategy:

- use symlinks while the migration is still active
- preserve traceability before deduplication
- move semantic authority to `atomos/`

Linked sources:

- `specs/`
- `docs_graphlang/`
- `desk_graphlang/`

This directory is not the canonical source.
