# Canonical Spec Docs

This directory is reserved for compiled, mature documents.

Source of truth:

- `atomos/`

Current migration rule:

- `atomos/` is being filled by decomposing the existing `specs/` corpus first
- new atoms should be introduced only when overlap or contradiction proves they are needed

Supporting migration archive:

- `specs_raw/`

These docs should remain derived, not primary-edit sources.
