# OntoMap Desk Standards

Use this `desk/` to turn the multidimensional graph-semantics model into auditable implementation work.

Rules:

1. Keep `ontomap` focused on semantic mapping, dimensions, and projection semantics.
2. Separate core ontology definitions from converters and projections.
3. Every task must name the source format and target format clearly.
