Repository coding standard: every file starts with a module docstring; every class and function has a docstring. Heuristics: folders with more than 5 files should probably be split into subfolders; files over 80 lines should probably be split or reduced to a focused class; files with more than one class should probably be split into one class per file; classes with more than 2 functions should probably be split or rethought as abstract base plus focused implementations; functions doing more than 2 things should be split into smaller functions or a class; functions over 8 lines should probably be split; prefer small internal helper functions with descriptive names; repeated logic should become a shared abstraction or utility module; prefer typing, dataclasses, and clean interfaces; use class diagrams to validate responsibility boundaries and coupling.

Semantic note for future refactor completion: redesign Si states. `sinnlos` should mean contradiction or tautology, not pending/unmapped. Prefer a clearer label than `pending`; candidate term: `unmapped` (`no_mapeado`). `out_of_scope` should be treated as a subtype of `unsinnig`. `blocked` likely collapses into contradiction rather than a separate state. Consider representing sense states as dedicated dataclasses rather than plain enum strings once the current structural refactor is complete.