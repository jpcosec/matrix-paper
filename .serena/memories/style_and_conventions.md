Language: Python >=3.10, packaged with setuptools (`setup.py`), dependency on `pyyaml`, tests use `pytest`. Style is lightweight and pragmatic: snake_case names, small classes with imperative methods, plain dict-based data structures, dataclass-like context objects in some modules, limited type hint usage, minimal/no docstrings, and direct asserts in tests. The code mixes English identifiers with domain terms from German/Spanish logic vocabulary such as `sinnvoll`, `unsinnig`, and sample data in Spanish.